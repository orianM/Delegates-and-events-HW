namespace Delegates_and_events_HW
{
    delegate void MyFirstDelegate();
    delegate void Greet(string name);
    delegate int AddOrSub(int a, int b);
    delegate bool FilterDelegate(Student student, string myString);
    delegate bool ContainDelegate(Student student);
}